TODO ---

- consider service that requires websockets - how would a connection work
  through the gateway?

- profile service should:
  - create queue "profile"
  - bind queue to exchange "account" with routing key "account.delete"
